# COMO E ONDE EXECUTAR JAVASCRIPT 

* No navegador na aba console
    * document.body.style.backgroundColor = "blue"  //mudar as cores das páginas 
    * document.body.style.transform = "rotate(45deg)"; // Rotaciona o conteúdo em 45 graus
    * var novoParagrafo = document.createElement("p");
    novoParagrafo.textContent = "Este é um novo parágrafo adicionado dinamicamente!";
    document.body.appendChild(novoParagrafo);

    * navigator.geolocation.getCurrentPosition(function(posicao) {
    console.log("Latitude:", posicao.coords.latitude);
    console.log("Longitude:", posicao.coords.longitude);
    });

* https://codepen.io/pen/
    * ferramenta que possibilita você executar código html css e js no seu navegador

* Pelo Vs Code 
    * (criando uma página html e uma página js)
    * colocando a tag script direto no código 

